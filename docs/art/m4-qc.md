# Batch 4 / 角色资源质检

执行日期：2026-08-14  
范围：8 个 Boss 立绘、9 个 NPC 立绘、通用 NPC 地图精灵。

| 检查项 | 结果 |
|---|---|
| Boss 输出 | 8 张 `1024×1024` 透明 PNG，主体四周保留约 10% 画布留白 |
| NPC 输出 | 9 张 `512×512` 透明 PNG，主体四周保留约 10% 画布留白 |
| 地图 NPC | `sprites/npc.png` 为 `32×32` 透明 PNG |
| 角色完整性 | 全身、手持物、尾部/披风等轮廓未被裁切 |
| 画风 | 统一为清新幻想、深蓝灰描边、左上暖光 / 右下冷光 |
| 接入 | Boss 详情和 NPC 对话框已使用对应资源路径 |

## 结论

通过。下一批 Battle VFX 的载体格式尚未裁决，按执行计划保持 `BLOCKED`，不生成无法接入的伪资源。
