# Batch 8 全量资源 QC

## 静态资源校验

- 宠物：27 张 `1024×1024` 透明立绘，81 张 `256/128/64` 透明图标。
- Boss / NPC：8 张 `1024×1024` 透明 Boss 立绘，9 张 `512×512` 透明 NPC 立绘。
- 地图：13 个 `32×32` 透明对象与派生精灵，4 套按 `32px` 网格对齐的 Tileset。
- VFX：19 个属性模板和 12 个功能模板，均为 `1024×256` 透明 PNG 精灵表。
- UI：9 个属性、24 个状态、20 个技能类型图标及 1 个徽记；尺寸与透明角均通过校验。
- Batch 7：35 个道具、6 张战斗背景、1 张主页背景、6 张区域缩略图和 6 张事件插画的数量与尺寸均通过校验。
- 2026-08-15：10 张被动技能书图标已重生成并替换为 `256×256` RGBA PNG；全部透明角和可见主体检查通过。

## 技能池视觉适配

- `frontend/scripts/verify-skill-art-adaptation.ps1`：通过；核验 85 个主动技能、24 个被动技能（14 个固有/升级 + 10 个技能书），并确认新增 24 个主动、9 个固有被动和 10 个技能书被动均有可用的 UI 图标与透明 PNG 精灵表映射。
- 攻击类技能优先播放属性特效；隐匿、再生与被动反击分别复用暗属性、增益和对应属性的小型精灵表。

## 构建验证

- `vue-tsc -b`：通过。
- `vite build`：通过。
- `mvn test`：2026-08-15 复跑 507 项通过、零失败；配置加载确认 85 个主动技能、24 个被动技能（14 个固有/升级 + 10 个技能书）。
- 已知提示：ExploreView 产物约 1.70 MB，Vite 报出既有分包体积警告；本次未引入新依赖或额外分包策略。
- 未启动本地后端与 MySQL，因此游戏内实机截图留在阶段 14 的九大核心场景总验收执行。
