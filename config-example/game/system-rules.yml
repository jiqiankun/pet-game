# ============================================================
# 宠物精灵游戏 - 系统规则配置 (外部覆盖示例)
# ============================================================
# 将此文件放置在 ./config/game/ 目录下，启动时自动覆盖 JAR 内部默认值。
# 修改后需重启生效。
# 仅列出需要覆盖的字段，未列出的字段使用默认值。
# ============================================================

configVersion: 1

# 如需调整克制倍率，取消注释并修改：
# advantageMultiplier: 1.50
# disadvantageMultiplier: 0.75
# sameElementBonus: 1.20

# 如需调整暴击参数：
# critRate: 0.05
# critMultiplierMin: 1.4
# critMultiplierMax: 2.0

# 如需调整等级上限：
# levelCap: 50
