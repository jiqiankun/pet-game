# AGENTS.md — 宠物精灵游戏项目开发规范

本文件是本项目 AI 辅助开发的项目规范与约束说明。任何参与本项目开发的 AI 或开发者，**开始任何开发任务前必须先阅读本文件与「权威文档清单」中的全部文档**。

---

## 1. 项目概览

宠物精灵游戏是一款**个人开发、个人部署、个人游玩**的轻量单机 Web 宠物收集养成游戏，核心玩法为：宠物收集 + 自由培养 + 队伍构筑 + 回合制战斗 + 区域探索 + Boss 挑战。

核心设计原则：

> 系统可以有深度，但操作和实现不能过重。

当前开发目标为需求文档定义的**第一阶段**完整内容（27 宠物、5 区域、多难度 Boss、图鉴、任务、成就、存档备份、开发者模式）。

## 2. 权威文档清单（开发前必读）

| 文档 | 职责 |
|---|---|
| `宠物精灵游戏第一阶段需求设计文档 V1.0.md` | 玩法规则、数值规则、内容规模的唯一依据 |
| `宠物精灵游戏第一阶段技术方案说明 V1.0.md` | 技术栈、架构边界、部署方式、数据方案的唯一依据 |
| `宠物精灵游戏分阶段开发规划 V1.0.md` | 开发顺序、阶段范围、验收标准、阶段约束的唯一依据 |
| `AGENTS.md`（本文件） | 项目级开发规范与 AI 协作约束 |
| `docs/FRONTEND_STANDARDS.md` | 前端编码规范、组件约定、Phaser 边界 |
| `docs/BACKEND_STANDARDS.md` | 后端编码规范、模块结构、配置与事务约定 |
| `docs/TESTING_STANDARDS.md` | 测试规约、测试重点、阶段验收规则 |

**文档优先级：** 用户当前明确要求 > 需求/技术文档 > 分阶段开发规划 > 本文件其余内容 > 历史结论。文档之间出现冲突时，**必须上报并等待确认，禁止自行选择一方并隐式修改需求**。

## 3. 技术栈与架构（已确定，禁止重新设计）

- 前端：Vue 3 + TypeScript + Vite + Pinia + Vue Router（**Hash 模式**）+ Phaser（2D 表现）+ Tiled（JSON 地图）。
- 后端：Java 21 + Spring Boot 3.5.x，单体模块化（不拆微服务、不多 Maven 模块）。
- 数据：MyBatis-Plus + MySQL 8.4 LTS，Flyway 管理表结构变更。
- 接口：REST JSON，统一前缀 `/api/**`，统一响应结构 `{ success, data, message, code }`，业务错误使用稳定 errorCode。
- 部署：前端构建产物打入 Spring Boot 静态资源，输出单个可执行 `pet-game.jar`，`java -jar` 运行，默认监听 `127.0.0.1:8080`。
- 仓库：单 Git 仓库，目录为 frontend / backend / docs / scripts / config-example。

**架构边界（必须遵守）：**

> Vue 负责页面，Phaser 负责游戏表现，Spring Boot 负责业务规则，BattleEngine 负责全部战斗计算，MySQL 只存玩家存档，YAML 只存游戏内容配置。

- 内容配置与玩家存档严格分离；玩家数据只保存引用（如 species_id、skill_id），不复制配置内容。
- Phaser 不是业务系统：宠物数据、图鉴、背包、任务、存档、战斗公式一律不得写进 Phaser 场景。
- 战斗临时数据只存服务器内存；战斗结束统一结算落库；服务重启后未完成战斗直接丢弃。
- 手动/自动/普通/精英/Boss 战斗**全部使用同一个 BattleEngine**，唯一差异是「谁决定行动」；禁止出现多套战斗逻辑。
- 前端只提交行动意图，不提交计算结果；伤害、金币、经验、捕捉结果一律由后端计算。

**明确禁止引入：** 微服务、Spring Cloud、Redis、Kafka/RabbitMQ/RocketMQ、Elasticsearch、WebSocket、Kubernetes、Docker、Nginx、分布式事务、分库分表、复杂 DDD 框架、事件总线基础设施、CQRS、Event Sourcing、配置中心、登录鉴权/多用户体系、任何文档未要求的新基础设施。

## 4. 构建与运行

- 开发模式：前端 `npm run dev`（Vite 代理 `/api` → `localhost:8080`），后端以 Spring Boot 本地启动，MySQL 本地实例。
- 正式构建：统一构建脚本执行「前端 build → 清理并复制 dist 到后端静态资源 → Maven package → 输出 release 目录」。
- 正式运行只需 Java 21 + MySQL 8.4 + `pet-game.jar`；玩家环境不需要 Node。
- 数据库结构变更只允许通过 Flyway 迁移文件，**禁止手工改表**。

## 5. 分阶段开发规则

项目按《宠物精灵游戏分阶段开发规划 V1.0.md》划分为 **阶段 0 ～ 阶段 12**。每次开发任务必须：

1. **先读文档**：阅读需求文档、技术方案、分阶段开发规划与本文件及前后端编码规范与测试规约。
2. **明确当前阶段**：确认任务对应的阶段编号，以该阶段「实现范围」为唯一工作清单。
3. **检查前置依赖**：前置阶段的完成标准必须已满足；不满足时先报告，不自行补建。
4. **只实现当前阶段**：对照该阶段「本阶段不包含」逐项自查；不得因「顺便实现比较方便」提前实现后续系统。
5. **不修改无关模块**：不重构未涉及代码；确需变更表结构时随 Flyway 新增迁移并说明理由。
6. **不扩展需求**：发现设计缺失时执行「标记问题 → 给出建议 → 等待确认」，不直接实现。
7. **测试验证**：实现过程中按《docs/TESTING_STANDARDS.md》编写测试；完成后逐条对照该阶段「完成标准」与「核心业务规则」验证。
8. **验收自查**：汇报已完成项、验证结果、遗留问题、风险。
9. **更新进度**：阶段验收通过后，更新本文件「当前阶段状态」一节。
10. **同步更新文档**：每个开发阶段验收通过后，必须同步更新以下文档（**文档更新是阶段验收的必要条件，未更新文档视为验收未通过**）：
    - `README.md`：更新项目进度、已完成阶段、当前状态等信息。
    - `AGENTS.md`：更新「当前阶段状态」（§6）及相关约束。
    - `docs/FRONTEND_STANDARDS.md`：如本阶段涉及前端新增约定或技术变更，同步更新。
    - `docs/BACKEND_STANDARDS.md`：如本阶段涉及后端新增约定、模块结构或数据库规范变更，同步更新。
    - `docs/TESTING_STANDARDS.md`：如本阶段新增测试重点或验收规则，同步更新。

**阶段验收通过前，不开启下一阶段开发。**

## 6. 当前阶段状态

- 当前开发阶段：**阶段 11（成就、统计与完成度系统）— 已完成**
- 已完成阶段：阶段 0（工程脚手架）、阶段 1（配置体系）、阶段 2（存档 + 新游戏 + Bootstrap + 前端首页）、阶段 3（BattleEngine 统一引擎、伤害结算链路、技能/状态/被动配置体系、战斗 REST 接口、前端基础战斗页面）、阶段 4（战斗结算落库、公共经验池、五种升级、等级成长公式、自由加点/洗点、技能解锁装配、宠物详情页、队伍编辑、背包恢复道具、前端养成闭环）、阶段 5（野生遭遇生成、捕捉计算与捕捉/逃跑行动、三档捕捉球、捕获率实时显示、捕捉去向选择、仓库筛选排序/锁定收藏/昵称、放生与临别礼物、27 宠物种族配置补齐）、阶段 6（Phaser 集成与 Tiled 地图管线、起始据点+初始区域+森林区域、刷新组关联、野怪简单行为、HP 持续消耗、营地恢复/传送、区域解锁与移动、地图刷新、采集/宝箱、5 套队伍预设、战败流程、大地图雏形）、阶段 7（Boss 配置体系 2 Boss × 3 难度、控制抗性与连续衰减、阶段机制、Boss AI、Flyway V5 迁移 5 张进度表、Boss 模块实体/mapper/Service/Controller、BattleService Boss 集成、runFullBattle 自动挑战、幸运值系统、前端 BossView/Boss store/BattleView 扩展）、阶段 8（图鉴研究进度 + 历史记录、Flyway V6 迁移 2 张表、研究等级 Lv.0~5 配置化、逐级信息解锁、研究值来源 11 种、Lv.5 野外识别、PokedexService/PokedexController、既有行为接入、前端图鉴页面）、阶段 9（任务系统主线 12+支线 10+隐藏 3、NPC 对话、新手教学、6 Boss、3 新区域实装、前端任务页面/对话框/教学浮层、事件钩子集成、Flyway V7 迁移 6 张表）、阶段 10（道具体系补齐、商店系统、技能书系统、精英个体、特殊外观多变体、随机事件、隐藏遭遇与埋伏、推荐 Build、战斗加速 1x/2x/3x、Flyway V8 迁移 2 张表、前端商店页/技能书管理/推荐方案/事件对话框）、阶段 11（成就系统、玩家统计、Boss 挑战目标、游戏完成度、宠物履历、Flyway V10 迁移 3 张表 + player_pet 履历字段、前端成就/统计/完成度/宠物记录页面）
- 当前阶段：阶段 11（成就、统计与完成度系统）已完成
- 下一阶段：阶段 12（以规划文档为准）
- 阶段 11 实现说明：成就系统（24 成就、7 分类、事件记录驱动、20 条件类型、奖励含金币/经验/捕捉球/材料/称号/头像/徽章、REQUIRES_NEW 不阻断主流程、AchievementService/AchievementController）；玩家统计（23 统计键、increment/setMax/getAllStats/computeMostUsed、StatisticsService/StatisticsController）；Boss 挑战目标（boss-challenges.yml 5 主 Boss × 4 目标，4 类型判定 TURN_LIMIT/NO_RECOVERY_ITEM/NO_PET_FAINTED/MULTI_ELEMENT，单目标奖励 + 集齐称号，BossChallengeService/BossChallengeController）；游戏完成度（8 项权重 20/10/10/25/10/15/5/5 加权，CompletionService/CompletionController）；宠物履历（player_pet 滞外战斗字段 + PetHistoryService 战斗事件聚合）；V10 迁移 player_achievement/player_statistic/player_boss_challenge 3 张表 + player_pet captured_map_id/captured_at 字段；前端成就页/统计页/完成度面板/Boss挑战页/宠物记录标签 + store + types；事件钩子接入 Battle/Map/Quest/Pet/Storage 结算点；后端 424 单元测试全通过 + 前端构建通过
- 阶段 9 实现说明：主线 12 节点（含水域/雷域并行分支）+ 支线 10 + 隐藏 3 任务；NPC 对话系统约 10 个 NPC；新手教学 8 步；6 个 Boss（水域/雷域/遗迹主 Boss + 初始区域隐藏/森林精英/遗迹隐藏）；3 新区域实装（水域/雷域/遗迹）；前端任务页面/对话框/教学浮层/首页主线摘要；事件钩子接入战斗/地图/NPC；V7 迁移 6 张表 + story_completed 字段
- 阶段 10 遗留说明：净化药（ITEM_PURIFY_POTION）为配置预留（itemType 暂记 KEY_ITEM），待战斗内道具行动开放后启用；随机事件 TRIGGER_CAPTURE 结果目前复用遭遇战斗入口；埋伏点（ambushSpots）配置结构已就位但尚未在 maps.yml 填充具体埋伏数据（隐藏遭遇组已配置）；商店无每日刷新/限购（明确不做）
- 阶段 11 遗留说明：Boss 挑战目标仅判定 5 个已实装主 Boss，隐藏/精英 Boss 不设目标（符合规划）；完成度「隐藏区域」分项由于无独立隐藏区域实体，以隐藏/精英 Boss 击败比例近似（权重 5%）；完成度不要求 S 资质/特殊外观/稀有技能/Boss 噩梦全通（满足 100% 判定口径）；成就/统计/Boss挑战/完成度/履历均只作展示与奖励，不反向影响战斗数值；config-example 仅含 system-rules.yml 完成度权重示例，成就/Boss挑战等游戏内容配置随 JAR 打包不在外部示例内
- 阶段 8 遗留说明：进化资料仅占位（进化真实资料属后续阶段）；图鉴奖励发放属后续阶段；图鉴研究值全部 11 种来源已配置化（system.yml pokedex 段），规则结构不得自行变更；Lv.5 野外识别返回资质预估等级标签（S/A/B/C/D），不泄露完整六维资质；历史记录放生不清除；研究等级门槛严格递增校验由 GameConfigValidator 启动时强制
- 阶段 7 遗留说明：当前实装 2 个主 Boss（青草原 BOSS_MEADOW_GUARDIAN + 翠树林 BOSS_FOREST_KING），其余 3 个主 Boss + 2～3 隐藏/精英 Boss 随后续区域开放补齐（阶段 8～11）；BOSS 解锁类型区域（`unlockType: BOSS`）已在地图配置中预留但本阶段未使用，后续区域实装时启用；幸运兑换成本（100 点）与兑换数量（exchangeQty）均为配置化，规则结构不得自行变更
- 阶段 6 遗留说明：水域分支/雷属性分支/最终遗迹区域为结构预留（planned: true），阶段 9 开放实际内容；NPC 仅为地图占位对象（阶段 9 对话与任务）；Boss 入口已启用（阶段 7 实装，ExploreView boss:touch 导航至 /boss）；隐藏遭遇/随机选择事件/精英个体属阶段 10；简化遭遇入口（`POST /api/wild/battles`）仍保留供开发者模式使用，地图内遭遇通过 `MapExplorationService.validateEncounterGroup` 桥接至同一 WildEncounterService；营地传送触发地图刷新（视为重新进入区域）；地图美术为占位 PNG 资源，验收前不阻塞功能
- 阶段 5 遗留说明：简化遭遇入口（`POST /api/wild/battles` + 探索页按钮）为临时验收手段，阶段 6 由地图承接且不得在其中沉淀地图耦合逻辑；捕捉球正式获取途径（商店/掉落）属阶段 10，当前仅新游戏赠送 + 开发者模式临时补充入口；精英个体捕捉倍率字段已配置化（×0.6）但本阶段无精英个体（固定 1.0）
- 需求模糊点：已裁决完成，见规划文档 §9.3（决策一~七 + 决策八：恢复道具仅战斗外使用、战斗内无道具行动 + 决策九：技能/状态体系补充设计的阶段归属与回归处理）；阶段 5 补充裁决：捕捉球新游戏赠送（10/5/2）、逃跑必定成功同战败结算、野生奖励按遭遇组配置（被捕捉宠物不参与）、初始宠物等级 5 级（防初始打不过野生宠物，HP/技能/面板按 Lv.5 计算）、新游戏赠送小型恢复药 ×10 与复苏药 ×2；数值均配置化，规则结构不得自行变更
- 技能/状态体系补充设计回归修订：已按《plans/前五阶段开发修订计划.md》完成阶段 3～5 存量代码回归（REV-001～022 全部 DONE：技能来源/类型两维度、Effect 组合框架、状态五类模型与新状态、被动触发时机与防递归、升级自动装备规则、被动等级解锁、留生一击与震慑捕捉联动、新游戏赠送留生一击；后端 200 测试全部通过）；后续阶段开发直接按新规则执行

## 7. 固定规则速查（不得擅自修改）

- 9 属性、每宠单属性；克制 ×1.50 / 被克 ×0.75；本属性技能 ×1.20。
- 27 种基础宠物（稀有度 12/9/5/1），Lv.50 上限，六维属性，资质 0～100。
- 6 宠携带、3 宠上场、5 套预设；每回合每宠一次行动，速度决定顺序且每回合重算。
- 技能携带：自身主动最多装备 4 个 + 技能书主动最多携带 2 个（技能书主动最多学习 10 个，超限需遗忘）；被动技能无上限、掌握后自动生效；技能来源（INNATE/BOOK）与类型（ACTIVE/PASSIVE）必须分字段表达。
- 宠物自身技能：标准 6 主动 + 3 被动（主动范围 5～7、被动内容建议 2～5），Lv.1～40 升级自动解锁（不消耗金币/技能点）；不同宠物允许共享 skillId，实际 SkillDefinition 控制约 80～100 个、禁止复制技能定义。
- 暴击率 5%、倍率 1.4～2.0 均匀随机；治疗不暴击；正常命中最低 1 点伤害。
- 无 MP，技能以冷却限制；技能冷却/Buff/异常不跨战斗保留，HP 跨战斗保留。
- 【留生一击】致死时将可捕捉目标 HP 保留为 1（暴击不可绕过）并附加震慑（仅提供安全捕捉窗口，不提高捕获率）；触发保护时才清除持续伤害状态；Boss 正常受伤但不附加震慑。
- 吸血按实际 HP 损失计算（护盾吸收/过量伤害不计），DOT/反击/反射默认不吸血，受禁疗影响。
- 【命运天平】HP 百分比交换不是伤害；Boss 采用交换幅度上限（默认单次最多 20 个百分点）而非免疫。
- 战败零惩罚；营地恢复免费；地图刷新不依赖现实时间。
- 放生培养加成上限 50%；幸运值每 100 兑换一次不封顶；Boss 掉落情报 3/6/10 次解锁。
- 捕捉球仅 3 档；自动战斗仅限已击败过的敌人（Boss 分难度）；自动挑战不是扫荡。
- 捕捉公式 = 基础捕获率 × HP 系数 × 异常加成 × 球倍率 × 精英系数，不添加文档之外修正项；被捕捉宠物不结算击败掉落、不参与野生奖励；锁定宠物禁止放生，放生礼物价值点数底线规则（决策七）。
- 洗点第一阶段免费；Boss 无门票/体力/次数/冷却限制。
- 恢复道具仅在战斗外使用（背包使用），战斗内无道具行动（用户裁决）。
- 内容规模上限：5 区域 + 1 据点、5 主 Boss + 2～3 隐藏、主线 10～15、支线 8～12、成就 30～50、技能约 80～100、道具约 40～60，达到后不扩充。

## 8. 明确不做清单（第一阶段）

进化/突破实际玩法、双属性、繁殖、遗传、性格、复杂装备、公会、好友、PVP、交易、拍卖行、多人联机、排行榜、每日/周常任务、签到、体力、门票、限时活动、商店刷新、昼夜/季节、真实时间刷新、复杂天气、3D、玩家职业/战斗等级、多结局、复杂 GM 后台、活动中心/公告/商城弹窗、扫荡、自动探索、复杂元素反应网络、独立 App。

## 9. 汇报格式（每个开发任务结束时）

- 当前阶段与任务范围；
- 已完成内容（对照阶段实现范围）；
- 验收结果（逐条对照完成标准：通过/未通过/待确认）；
- 测试验证结果（已编写/已通过的测试情况）；
- 遗留问题与待确认需求；
- 是否建议进入下一阶段。
